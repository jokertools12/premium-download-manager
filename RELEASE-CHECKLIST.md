# ✅ قائمة فحص الإصدار السريع — Premium Download Manager

> اتبع بالترتيب قبل كل إصدار نسخة جديدة للمستخدمين.

## قبل البناء
- [ ] عدّل رقم الإصدار: `npm version patch` (أو minor / major)
- [ ] راجع CHANGELOG/ملاحظات الإصدار التي ستكتبها
- [ ] `npm start` — جرّب الميزات الجديدة يدوياً
- [ ] شغّل: `node test/engine-test.js` (التحميل الأساسي)
- [ ] شغّل: `node test/queue-test.js` (بدون إنترنت — سريع)
- [ ] تأكد أن `publish.owner` في `package.json` = حساب GitHub الصحيح

## البناء والنشر
- [ ] `npm run clean`
- [ ] `set GH_TOKEN=رمزك` (لخيار GitHub)
- [ ] `npm run dist` (بناء محلي للتجربة) أو `npm run release` (بناء + نشر)
- [ ] تحقق من وجود الملفات الثلاثة في dist: **Setup exe + latest.yml + .blockmap**
- [ ] ثبّت النسخة التجريبية من exe على جهازك واختبرها
- [ ] ارفع/انشر: exe + latest.yml + blockmap في نفس الـ Release

## بعد النشر
- [ ] ثبّت النسخة القديمة على جهاز آخر → تأكد أن التحديث يظهر ويعمل
- [ ] اكتب ملاحظات الإصدار (الميزات الجديدة + الإصلاحات)
- [ ] Publish الـ Release على GitHub

## ملاحظات
- أول إصدار فقط: المستخدمون يحتاجون تنزيل exe يدوياً — التحديثات التالية تلقائية
- بدون توقيع رقمي: سيظهر تحذير SmartScreen عند أول تثبيت — المستخدم يضغط "Run anyway"
- تحديث النسخ المثبتة يتطلب `latest.yml` مطابقاً لرقم الإصدار في package.json
