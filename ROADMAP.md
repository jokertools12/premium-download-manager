# 🗺️ خارطة طريق التطوير الاحترافية — Premium Download Manager

> خطة شاملة مبنية على تدقيق فعلي للكود الحالي (v1.0.2) — تُحدَّث مع كل إصدار

---

## 📊 أولاً: تقييم الوضع الحالي (تدقيق الكود)

### ✅ ما لديك الآن (نقاط القوة)
| المجال | الحالة |
|---|---|
| **محرك التحميل** | تقسيم Segments + استئناف + إعادة محاولة (5 مرات) + محدد سرعة + Mirrors |
| **الفيديو** | yt-dlp (تنزيل تلقائي) + ffmpeg دمج + HLS/M3U8 |
| **التورنت** | WebTorrent كامل مع فحص الماغنت |
| **المتصفح** | إضافة Chrome/Edge/Firefox + Native Messaging |
| **البيانات** | SQLite (kv/tasks/resume/stats) + تراجع JSON |
| **الواجهة** | عربية RTL + داكن/فاتح + 3 لغات + نافذة عائمة + Tray |
| **النشر** | تحديث تلقائي + سكربت release آلي بنقرة واحدة |

### ⚠️ الفجوات المكتشفة (فرص التطوير)
1. **`app.js` ملف واحد ضخم (52KB)** — rendering عبر `innerHTML` (أداء + مخاطر أمان مع نصوص خارجية)
2. **IPC بقمث نصي واحد** (`pdm` + case strings) — صعب الصيانة والتوسع
3. **لا توجد اختبارات آلية في `package.json`** (مجلد test يدوي فقط) ولا CI
4. **المهام مخزنة كـ JSON Blobs** — لا بحث/فرز/إحصاءات SQL حقيقية
5. **التوقيع الرقمي مفقود** — SmartScreen يحذر المستخدمين
6. **مميزات IDM ناقصة**: Site Grabber، معاينة فيديو قبل التحميل، ZIP Preview، تسجيل دخول مواقع، FTP/WebDAV
7. **Windows فقط** — لا macOS/Linux

---

## 🎯 الرؤية
> **"أن يكون الخيار الأول المجاني مفتوح المصدر لمدير التحميل على ويندوز — أسرع من IDM، أجمل منه، وبلا إعلانات"**

---

## 🚀 المراحل الست

### المرحلة 1 — v1.1 «الأساس المتين» ⭐ (أولوية قصوى P0)
> البنية قبل المميزات — كل ما بعدها يبنى على هذه المرحلة

> **الحالة: ✅ مكتملة (9 فبراير 2026)**
> - 25 اختباراً آلياً ناجحاً (Vitest) تغطي: محرك التحميل ضد خادم HTTP حقيقي (segments/resume/mirrors/403/Content-Disposition)، SpeedLimiter، Database، i18n، sanitize
> - ESLint: صفر أخطاء | CI/CD على GitHub Actions (test+lint عند push، بناء تلقائي عند tag)
> - **اكتشافات الاختبارات (أخطاء حقيقية أُصلحت):** (1) تعليق SpeedLimiter للأبد عند طلب بايتات > rate، (2) off-by-one في نهاية المقطع للخوادم بدون دعم Range — كانت تسبب حلقة إعادة محاولة لا نهائية، (3) fs مفقود في TorrentManager.remove
> - الأمان: escapeHtml/escapeAttr/escapeUrl إلزامية عبر src/renderer/lib/sanitize.js ومطبقة على بطاقات المهام، نتائج yt-dlp، وملفات التورنت
> - توثيق IPC: src/main/ipc-map.js هو المرجع المركزي المُوثّق لأوامر قناة `pdm`

| # | المهمة | تفاصيل تقنية | الجهد |
|---|---|---|---|
| 1.1 | ✅ **اختبارات آلية** | Vitest + خادم HTTP محلي حقيقي: segments/resume/mirrors/403/filename | منجز |
| 1.2 | ✅ **CI/CD على GitHub Actions** | `.github/workflows/ci.yml`: lint+test عند push، بناء exe ونشره عند tag | منجز |
| 1.3 | ✅ **تفكيك `app.js`** | 14 وحدة ES Modules (state + lib/{format,dom} + ui/*9 + events) + بروتوكول `app://` (وحدات ESM تفشل عبر file:// مع مسافات في المسار) + فحص دخان `test/renderer-smoke.js` | منجز |
| 1.4 | ✅ **أمان `innerHTML`** | `sanitize.js` (escapeHtml/escapeAttr/escapeUrl) + تطبيق على النقاط الخطرة | منجز |
| 1.5 | ✅ **توثيق IPC** | `src/main/ipc-map.js` مرجع مركزي موثّق JSDoc | منجز |
| 1.6 | ✅ **ESLint + Prettier** | flat config + `.prettierrc` + فحص في CI | منجز |

**معيار نجاح المرحلة:** `npm test` أخضر في CI، ولا أخطاء ESLint، والبناء تلقائي على GitHub.

---

### المرحلة 2 — v1.2 «محرك من فئة عالمية» 🔧
> هنا نتفوق على IDM تقنياً

> **الحالة: ✅ مكتملة (9 فبراير 2026)**
> - 59 اختباراً آلياً ناجحاً (18 جديداً للمحرك): تكيف الاتصالات، checksum + إصلاح المقاطع، التسمية الذكية، FTP، التخصيص المسبق
> - **اكتشاف الاختبارات (خطأ حقيقي أُصلح):** parseChecksum لم يكن يقبل الصيغ بالأحرف الكبيرة (SHA-256=...) — التعبير النمطي كان بلا علامة i
> - الاتصالات التكيفية (2.1): تجمّع عمال Workers يبدأ بـ4 اتصالات؛ عند أداء ممتاز يقسم أكبر مقطع حر ويضيف عاملاً (حتى maxConnections)، وعند اختناق يخفض اتصالاً واحداً بلطف مع حفظ التقدم — دون كسر صيغة الاستئناف
> - المجموع الاختباري (2.2): sha256/sha512/sha1/md5 عبر حقل checksum في أمر add؛ عند عدم التطابق يعيد جلب كل مقطع ويقارنه بايت-ببايت ويعيد كتابة المتفاوت فقط، ثم يعيد التحقق — والفشل النهائي فوري بلا إعادة محاولات
> - فك الأرشيف (2.3): ZIP/RAR/7z عبر 7-Zip/WinRAR أو PowerShell المدمج، إعداد autoExtract + إشعار في الواجهة
> - FTP/FTPS (2.4): وحدة protocols/ftp.js بواجهة موحدة مع DownloadTask (استئناف REST + محدد السرعة المشترك) + WebDAV Basic Auth مشتق تلقائياً من user:pass@ في الرابط
> - التسمية الذكية (2.5): قوالب {date} {time} {site} {name} {ext} {category} {index} + uniquifyPath لحل تعارض الأسماء (لا يكسر الاستئناف)
> - التخصيص المسبق (2.6): اختبار يثبت أن الملف يشغل حجمة الكامل على القرص منذ بدء التحميل

| # | الميزة | تفاصيل تقنية | الأولوية |
|---|---|---|---|
| 2.1 | ✅ **اتصالات ديناميكية تكيفية** | تجمّع Workers + قياس سرعة/اتصال دوري + تقسيم أكبر مقطع حر + خفض لطيف بالإجهاد | P0 |
| 2.2 | ✅ **Checksums & سلامة** | sha256/sha512/sha1/md5 + إصلاح Segment تالف فقط (مقارنة بايت-ببايت) | P1 |
| 2.3 | ✅ **فك الأرشيف تلقائياً** | ZIP عبر PowerShell/7-Zip، RAR/7z عبر 7-Zip/WinRAR + إشعار Toast | P1 |
| 2.4 | ✅ **بروتوكولات FTP/FTPS/WebDAV** | protocols/ftp.js (basic-ftp) بواجهة موحدة + Basic Auth تلقائي من الرابط | P1 |
| 2.5 | ✅ **إعادة تسمية ذكية** | قوالب {date}_{site}_{name}… + حل تعارض الأسماء (1)(2) | P2 |
| 2.6 | ✅ **Pre-allocate الملف** | مُنفّذ مسبقاً في v1.0 (fd.truncate في _openFd) — أضيف اختبار | P0 ✅ |

---

### المرحلة 3 — v1.3 «واجهة تنافس المتطورة» 🎨

> **الحالة: ✅ مكتملة (9 فبراير 2026)**
> - 79 اختباراً آلياً ناجحاً (21 جديداً) + فحص دخان Electron: إقلاع نظيف 0 أخطاء console
> - الفرز الفوري (3.1): 5 مفاتيح + عكس الاتجاه عبر sortTasks نقية مُختبَرة
> - السجل الكامل (3.2): جدول history (سقف 1000) يُدوّن الاكتمال والفشل — عرض شهري مع فتح/إظهار/إعادة تحميل بنقرة/حذف
> - معاينة الوسائط (3.3): مسار `app://media` بقائمة امتدادات بيضاء صارمة + مصغرات الصور في البطاقات + مشغل داخلي (صورة/فيديو/صوت) — CSP محدّث
> - إشعارات Toast بأزرار إجراء (3.4): الإكمال ← فتح/إظهار، الفشل ← استئناف
> - اختصارات لوحة المفاتيح (3.5): Ctrl+N، Ctrl+F، أسهم التحديد، Space، Delete، Escape
> - لوحة إحصائيات غنية (3.6): مخطط أعمدة يومي (14 يوماً) عبر dailySeries نقية مع تعبئة الأيام الفارغة
> - سحب وإفلات للنافذة العائمة (3.7): إفلات رابط ← تفتح الرئيسية وتقترح الرابط
> - Theming متقدم (3.8): لون تمييز مخصص + كثافة عرض (مريح/مضغوط)

| # | الميزة | التفاصيل | الحالة |
|---|---|---|---|
| 3.1 | **بحث/فرز/تصفية فورية** | حقل بحث + فرز بالأعمدة | ✅ |
| 3.2 | **سجل التحميل الكامل** | تاريخ شهري + إعادة تحميل بنقرة | ✅ |
| 3.3 | **معاينة الوسائط** | مصغرات صور، مشغل فيديو/صوت داخلي | ✅ |
| 3.4 | **إشعارات Toast** | نجاح/فشل/اكتمال مع أزرار إجراء | ✅ |
| 3.5 | **اختصارات لوحة المفاتيح** | Ctrl+N, Space, Del, Ctrl+F | ✅ |
| 3.6 | **لوحة إحصائيات غنية** | رسوم بيانية يومية/أسبوعية | ✅ |
| 3.7 | **سحب وإفلات من المتصفح** | Drag رابط → أيقونة عائمة | ✅ |
| 3.8 | **Theming متقدم** | ألوان مخصصة + كثافة عرض | ✅ |

---

### المرحلة 4 — v1.4 «ملك الوسائط» 🎬

> **الحالة: ✅ مكتملة (9 فبراير 2026)**
> - 90 اختباراً آلياً ناجحاً (11 جديداً) + فحص دخان Electron: إقلاع نظيف 0 أخطاء console
> - **اكتشاف الاختبارات (خطآن حقيقيان أُصلحا):** (1) parseTimecode فسّر '2:15' كـ 2س+15ث = 7215 بدل 135 — أُعيدت كتابته بفروع صريحة حسب عدد المقاطع (ثوانٍ | د:ث | س:د:ث) و'90' حرة و'0:00' → 0، (2) تكرار وسائط progress-template/الرابط بين الباني و_run بعد الدمج — أزيل التكرار والرابط الآن دائماً آخر وسيطة، (3) `await` خارج دالة async في معالج نقر البطاقات جمّد الإقلاع — اكتشفه lint وفحص الدخان
> - **الوحدة النقية `integrations/ytdlp-args.js`**: بناء وسائط yt-dlp كاملة من خيارات المهمة — الرابط دائماً أخيراً
> - نافذة الفيديو المتقدمة (4.1): خيار صوت MP3 فقط + الترجمات + صيغة الدمج + قص المقطع
> - تحويل/دمج ffmpeg (4.2): `--merge-output-format mp4/mkv` مع تنزيل ffmpeg تلقائياً عند الحاجة
> - استخراج MP3 بنقرة (4.3): زر 🎵 في كل فيديو مكتمل → ffmpeg ببطاقة تقدم مباشرة (قراءة time= من ffmpeg) + إشعار نتيجة
> - قوائم التشغيل (4.4): أزرار تحديد الكل/إلغاء التحديد + اختيار مقاطع بالمدة (موجود من قبل وأُحسّن)
> - قص المقاطع (4.5): حقول من/إلى بصيغة 0:30 أو 1:02:03 → `--download-sections` (فيديو مفرد فقط، والنهاية يجب أن تتجاوز البداية)
> - الترجمات التلقائية (4.6): `--write-subs --write-auto-subs --sub-langs ar,en.*`

| # | الميزة | التفاصيل | الحالة |
|---|---|---|---|
| 4.1 | **نافذة معاينة الفيديو** | يوتيوب/تيك توك — اختيار الجودة + MP3 + ترجمة | ✅ |
| 4.2 | **تحويل صيغ ffmpeg** | MP4/MKV تلقائي عبر --merge-output-format | ✅ |
| 4.3 | **استخراج الصوت** | زر MP3 في أي فيديو مكتمل (ffmpeg خلفياً بتقدم) | ✅ |
| 4.4 | **Playlist كاملة** | قائمة يوتيوب → اختيار مقاطع + تحديد الكل/لا شيء | ✅ |
| 4.5 | **قص المقاطع** | بداية/نهاية قبل التحميل → --download-sections | ✅ |
| 4.6 | **Subtitles تلقائية** | --write-subs/--write-auto-subs بلغات ar,en | ✅ |

---

### المرحلة 5 — v1.5 «النظام البيئي» 🌐

> **الحالة: ✅ مكتملة (9 فبراير 2026)**
> - 104 اختباراً آلياً ناجحاً (14 جديداً) + فحص دخان Electron: إقلاع نظيف 0 أخطاء console
> - إضافة المتصفح v2.1 (5.1): **التقاط HLS/DASH** — webRequest يجمع روابط m3u8/mpd لكل تبويب (آخر 30) والـ popup يعرضها مع زر تحميل لكل بث، عبر جسر runtime.onMessage الجديد
> - تسجيل دخول المواقع (5.2): قائمة متصفح (Chrome/Edge/Firefox/Brave) في نافذة الفيديو → `--cookies-from-browser` عبر بنّاء الوسائط
> - Site Grabber (5.3): `integrations/grabber.js` نقي مُختبَر (img/source/video/audio + data-src الكسول + روابط الملفات، روابط مطلقة، منع تكرار، قائمة بيضاء للامتدادات) + نافذة فحص ثلاثية القوائم مع تحديد وتحميل جماعي (بث M3U8 يتوجه تلقائياً لمدخل الفيديو)
> - مزامنة البيانات (5.4): تصدير/استيراد JSON شامل (إعدادات + مهام + سجل + إحصاء) مع حوارات حفظ/فتح ودمج آمن (المهام الواردة تبدأ متوقفة) عبر `reloadFromDb` الجديد
> - CLI + URL Scheme (5.5): `cli.js` نقي — `PremiumDM.exe add <url>` أو رابط مباشر أو `pdm://add?url=...` مع تسجيل المخطط `setAsDefaultProtocolClient` ومعالجة first/second instance وتوجيه البث للمدخل الصحيح
> - نسخة Portable (5.6): أهداف بناء nsis + zip + portable بأسماء PremiumDM-Portable
> - API للمطورين (5.7): `GET http://127.0.0.1:45762/status` يعيد إصدار البرنامج وملخص المهام (يضاف لـ /ping و /add الموجودين)

| # | الميزة | التفاصيل | الحالة |
|---|---|---|---|
| 5.1 | **إضافة متصفح v2** | Popup + التقاط HLS لكل تبويب + كليك يمين | ✅ |
| 5.2 | **تسجيل دخول مواقع** | كوكيز المتصفح عبر --cookies-from-browser | ✅ |
| 5.3 | **Site Grabber** | فحص صور/فيديوهات/ملفات صفحة + تحديد وتحميل | ✅ |
| 5.4 | **مزامنة سحابية** | تصدير/استيراد JSON شامل | ✅ |
| 5.5 | **CLI + URL Scheme** | `add <url>` + `pdm://add?url=...` | ✅ |
| 5.6 | **نسخة Portable** | أهداف zip + portable في electron-builder | ✅ |
| 5.7 | **API للمطورين** | localhost /status + /ping + /add | ✅ |

---

### المرحلة 6 — v2.0 «الانطلاقة الكبرى» 🌍

> **الحالة: ✅ مكتملة (9 فبراير 2026)**
> - 113 اختباراً آلياً ناجحاً (9 جديدين) + فحص دخان Electron
> - المنصات الثلاث (6.1): وحدة `platforms.js` نقية — ثنائيات yt-dlp/ffmpeg لكل منصة (ffmpeg-static ثنائي مفرد بلا أرشيف)، chmod على POSIX، taskkill/kill حسب المنصة، مجلدات بيانات (APPDATA/Library/XDG)، Native Messaging عبر السجل على ويندوز ومجلدات المتصفحات على ماك/لينكس، أهداف بناء dmg/AppImage/deb، مصفوفة CI ثلاثية المنصات، وكشف المنصة للواجهة عبر preload
> - التوثيق الرقمي (6.2): `docs/SIGNING.md` — دليل EV وتفعيل CSC_LINK في CI (يتطلب شهادة مدفوعة)
> - الموقع الرسمي (6.3): `website/index.html` — صفحة هبوط كاملة بالعربية مع أزرار تحميل حية من GitHub Releases (قابلة للنشر عبر GitHub Pages)
> - المجتمع (6.4): `CONTRIBUTING.md` + قوالب Issues (بلاغ خطأ/اقتراح ميزة)
> - نظام الإضافات (6.5): `PluginManager` — اكتشاف من plugins-builtin + مجلد المستخدم، ctx محدود (addDownload/onTaskCompleted/onTaskFailed/getTasks/log)، تفعيل/تعطيل محفوظ، إضافة مثال مدمجة، قسم في الإعدادات، و`docs/PLUGINS.md`
> - الترجمة المجتمعية (6.6): `TRANSLATING.md` — دليل إضافة لغة كاملة + خطة Weblate

| # | الميزة | التفاصيل | الحالة |
|---|---|---|---|
| 6.1 | **macOS + Linux** | إعادة هيكلة كاملة للثلاث منصات + بناء تلقائي | ✅ |
| 6.2 | **توقيع رقمي EV** | دليل كامل + جاهزية CI (يتطلب شهادة مدفوعة) | ✅ |
| 6.3 | **موقع رسمي** | website/index.html جاهز لـ GitHub Pages | ✅ |
| 6.4 | **مجتمع ومساهمون** | CONTRIBUTING.md + قوالب Issues | ✅ |
| 6.5 | **متجر إضافات** | نظام Plugins كامل + إضافة مثال | ✅ |
| 6.6 | **ترجمة مجتمعية** | TRANSLATING.md + دليل Weblate | ✅ |

---

## 📅 الجدول الزمني المقترح

```
v1.1 (الأساس)      ████████░░░░░░░░░░░░  ~2-3 أسابيع
v1.2 (المحرك)      ░░░░████████░░░░░░░░  ~3-4 أسابيع
v1.3 (الواجهة)     ░░░░░░░░████████░░░░  ~3-4 أسابيع
v1.4 (الوسائط)     ░░░░░░░░░░░░██████░░  ~3 أسابيع
v1.5 (النظام)      ░░░░░░░░░░░░░░░░████  ~4 أسابيع
v2.0 (التوسع)      بعد استقرار v1.5     ~6-8 أسابيع
```

---

## 🧭 مبادئ التنفيذ

1. **لا ميزة جديدة قبل أن تكون الأساسيات مستقرة** — المرحلة 1 إلزامية
2. **كل ميزة تُقاس**: سرعة، استهلاك RAM، حجم installer (< 100MB)
3. **الاستئناف دائماً مقدّس** — أي تعديل في المحرك يجب ألا يكسر ملفات الاستئناف القديمة
4. **i18n من اليوم الأول** — أي نص جديد يدخل `i18n.js` بثلاث لغات فوراً
5. **التوافق للخلف** — مستخدم v1.0.2 يرقّى إلى v2.0 بلا فقدان بيانات

## 📈 مؤشرات النجاح (KPIs)
- ⚡ سرعة تحميل ≥ 90% من سرعة IDM
- 💾 استهلاك RAM < 200MB
- 🐛 Crash rate < 0.5%
- ⭐ تقييمات إيجابية + نمو التحديثات التلقائية
