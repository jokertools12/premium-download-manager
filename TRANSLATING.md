# 🌍 الترجمة — Premium Download Manager

البرنامج يدعم ثلاث لغات حالياً: **العربية، English، Türkçe**. ترحيب كبير بأي لغة جديدة!

## كيف تعمل الترجمة؟
كل النصوص في ملف واحد: [`src/renderer/i18n.js`](src/renderer/i18n.js)

كل مفتاح بالصيغة:
```js
'key.name': ['النص العربي', 'English text', 'Türkçe metin'],
```
والمعاملات تُحقن بأقواس: `'اكتمل: {name}'` ← `t('notif.done', { name })`.

## إضافة لغة جديدة (مثال: الفرنسية)
1. في `i18n.js` أضف عنصراً رابعاً لكل مفتاح: `'key': ['عربي', 'en', 'tr', 'français']`
2. أضف اللغة لقائمة الاختيار في `src/renderer/index.html` (حقل `#stLang`)
3. أضف `locale.tag` بقيمة رمز اللغة (`fr`) — يُستخدم لأسماء الأشهر
4. اختبر: `npm test` — هناك اختبار يتحقق من **تكافؤ المفاتيح بين كل اللغات**
5. أرسل PR — سيُدمج بسرعة إن اكتمل

## عبر Weblate
المشروع مفتوح للاستضافة على [Weblate](https://weblate.org/) المجانية للمشاريع مفتوحة المصدر:
- شكل الترجمة الحالي (مصفوفات JS) قابل للتحويل آلياً إلى JSON/PO لأداة Weblate
- بعد إعداد المستودع على Weblate تُفتح الترجمات الجماعية مع مراجعة

## قواعد
- لا تترجم حرفياً — ترجم **المعنى** بمصطلحات مديري التحميل المعروفة في لغتك
- حافظ على الرموز التعبيرية (تساعد على التعرف البصري السريع)
- لا تغير مفاتيح `{param}` داخل النص — فقط موضعها
